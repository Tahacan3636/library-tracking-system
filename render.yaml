services:
  - type: web
    name: library-tracking-system
    runtime: node
    plan: free
    buildCommand: npm install && npm run setup
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: BASE_URL
        sync: false
